namespace App.WindowsForm.Forms
{
    /// <summary>
    /// Enum to control AccountForm behavior (Add/Edit/View modes).
    /// Allows one form to handle three different use cases with different button labels and field editability.
    /// </summary>
    public enum AccountFormMode { Add, Edit, View }
}